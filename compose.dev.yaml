services:
  server:
    build:
      context: ./server
    image: spectre-server:dev
    ports:
      - "8080:8080"
    environment:
      - PORT=8080

  web-ui:
    build:
      context: ./web-ui
    image: spectre-web-ui:dev
    ports:
      - "3000:80"
    environment:
      - VITE_API_BASE=http://localhost:8080
