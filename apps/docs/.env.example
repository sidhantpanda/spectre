# Production documentation origin (optional on Vercel).
# The scheme is optional and defaults to https, so "docs.spectre.example.com"
# and "https://docs.spectre.example.com" both work.
NEXT_PUBLIC_SITE_URL=https://docs.spectre.example.com
